# Overview

Library for various integrity functionality.

## signer
Different signer types and supported keys for signing data

## vc
Creates Verifiable Credentials and proofs.
Requires the `signer` module for signing

